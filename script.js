const audio=document.querySelector('#audio'), box=document.querySelector('#lyrics'), startScreen=document.querySelector('#startScreen');const play=document.querySelector('#play'),start=document.querySelector('#start'),back=document.querySelector('#back'),forward=document.querySelector('#forward'),seek=document.querySelector('#seek'),fill=document.querySelector('#fill'),now=document.querySelector('#now'),dur=document.querySelector('#dur');const lines=window.LYRICS;let active=-1;
function fmt(x){x=Math.max(0,Math.floor(x||0));return Math.floor(x/60)+':'+String(x%60).padStart(2,'0')}
lines.forEach((l,i)=>{const d=document.createElement('div');d.className='line';d.dataset.i=i;d.textContent=l.text;d.onclick=()=>audio.currentTime=l.at;box.appendChild(d)});const els=[...document.querySelectorAll('.line')];
function sync(){let i=-1;for(let n=0;n<lines.length;n++){if(audio.currentTime>=lines[n].at)i=n;else break}if(i!==active){active=i;els.forEach((e,n)=>{e.className='line '+(n===i?'current':n<i?'past':'future')});if(i>=0)els[i].scrollIntoView({behavior:'smooth',block:'center'})}now.textContent=fmt(audio.currentTime);dur.textContent=fmt(audio.duration);fill.style.width=audio.duration?(audio.currentTime/audio.duration*100)+'%':'0%'}
async function toggle(){if(audio.paused){await audio.play();startScreen.classList.add('startHidden');play.textContent='Ⅱ'}else{audio.pause();play.textContent='▶'}}start.onclick=toggle;play.onclick=toggle;back.onclick=()=>audio.currentTime=Math.max(0,audio.currentTime-10);forward.onclick=()=>audio.currentTime=Math.min(audio.duration,audio.currentTime+10);audio.addEventListener('timeupdate',sync);audio.addEventListener('loadedmetadata',sync);audio.addEventListener('ended',()=>{play.textContent='▶';startScreen.classList.remove('startHidden');active=-1;sync()});seek.onclick=e=>{const r=seek.getBoundingClientRect();audio.currentTime=(e.clientX-r.left)/r.width*audio.duration};document.querySelector('#mute').onclick=e=>{audio.muted=!audio.muted;e.currentTarget.textContent=audio.muted?'×♫':'♫'};document.addEventListener('keydown',e=>{if(e.code==='Space'){e.preventDefault();toggle()}if(e.key==='ArrowLeft')back.click();if(e.key==='ArrowRight')forward.click()});sync();
